import React, { Component } from 'react'
import { Text, View } from 'react-native'

export default class activatePage extends Component {
    render() {
        return (
            <View>
                <Text> activate page (and choose modes) </Text>
            </View>
        )
    }
}
