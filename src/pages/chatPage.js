import React, { Component } from 'react'
import { Text, View } from 'react-native'

export default class chatPage extends Component {
    render() {
        return (
            <View>
                <Text> this is just one group or private message page </Text>
            </View>
        )
    }
}
