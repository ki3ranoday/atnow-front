import React, { Component } from 'react'
import { Text, View } from 'react-native'

export default class friendsPage extends Component {
    render() {
        return (
            <View>
                <Text> Contacts, friend requests, and search for friends </Text>
            </View>
        )
    }
}
